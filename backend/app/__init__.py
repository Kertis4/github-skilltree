"""GitHub SkillTree OAuth backend (FastAPI).

This package implements the **server-side half** of GitHub OAuth so the OAuth
*client secret* never reaches the browser. See ``app.main`` for the routes.
"""
